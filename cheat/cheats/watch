# Watch a command refreshing every 30 seconds and highlighting differences
watch -n 30 -d "wc -l cmrs.log"

# Watch with colors on (watch doesn't understand aliases, ie 'll', or bash setup, ie --color=always)
watch -c "ls -al --color"

