misc
* No Machine for Remote
* Dropwizard or Spring Boot
* Logback vs Log4j

Dependencies not found:
* vim
* wget (for mac)
* tree
* jq

todo:
* rm i3 related stuff
* improve install scripts for my dotfiles
* tattoo



One general comment, I believe we should use any() or any`<type`>() when setting up expectations ie when(mock.call(anyString(), any())).thenReturn("world")
Then we in the verify you explicitly state what values you expected ie verify(mock).call("hello", psr);
