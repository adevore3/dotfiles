# Now, every program that uses the readline library for tty input ( perl -d,
# the python REPL, psql, gdb, anything you run under rlwrap, etc.) has vi key
# bindings instead of the default emacs bindings.
#
# http://alangrow.com/blog/put-everything-in-vi-mode
set keymap vi
set editing-mode vi

