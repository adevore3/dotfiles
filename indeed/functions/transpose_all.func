#!/bin/bash

# Transpose all log entries
function transpose_all() {
  if [ $# -ne 1 ]; then
    echo "Error: No file specified."
    return 1
  fi

  local filename=$1
  if [ -f $filename ]; then
    cat $filename | transpose-log-entries
  else
    echo "'$filename' is not a valid file"
  fi
}

