#!/bin/bash

# Get the product group and repo name based on the path
function get_repo_product_group_and_name() {
  if [ ! -d ".git" ]; then
    echo "Error: Must be in a git directory"
    return 1
  fi

  echo $(basename $(dirname "$PWD"))/$(basename "$PWD")
}

